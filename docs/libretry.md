# retry

**retry** is a C library for running functions that may fail and should be tried again.

## Usage

## Build shared object

To build and install the shared object:

```sh
make
```

```sh
sudo make install
```

## Install shared object and headers

```sh
make
```

## Example 

Build the example:

```sh
make example
```
